# StakingRewards — frontend demo (Fase 6)

Ver playbook completo: [`../doc/FRONTEND.md`](../doc/FRONTEND.md).

```bash
cp .env.example .env.local   # addresses del deploy
npm install
npm run dev                  # http://localhost:3000
npm test
npm run build
```
