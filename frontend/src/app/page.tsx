import { StakingApp } from "@/components/StakingApp";

/**
 * Home: monta la demo cliente.
 * Server Component; la interacción vive en `StakingApp` (`'use client'`).
 */
export default function HomePage() {
  return <StakingApp />;
}
